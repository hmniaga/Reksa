@{
    ViewBag.Title = "Search CIF";
    string search = (string)ViewBag.Search;
    string criteria = (string)ViewBag.Criteria;

    Layout = null;
}
<script type="text/javascript">
    function onSearchCustomerRedemp() {
        var value = $("#CustomerRedempSearchComponentSearchTxt").val();
        var grid = $("#CustomerRedempSearchComponentGrid").data("kendoGrid");

        var parsedUrl = updateQueryStringParameter(grid.dataSource.transport.options.read.url, "search", value);
        grid.dataSource.transport.options.read.url = parsedUrl;

        grid.dataSource.page(1);
        grid.dataSource.read();
        grid.refresh();
    }

    function updateQueryStringParameter(uri, key, value) {
        var re = new RegExp("([?&])" + key + "=.*?(&|$)", "i");
        var separator = uri.indexOf('?') !== -1 ? "&" : "?";
        if (uri.match(re)) {
            return uri.replace(re, '$1' + key + "=" + value + '$2');
        }
        else {
            return uri + separator + key + "=" + value;
        }
    }

    function onRowCustomerRedempSelect(e) {
        var data = this.dataItem(this.select());
        var CIFNo = $.trim(data.CIFNo);
        var CIFName = $.trim(data.CIFName);
        var NasabahId = $.trim(data.NasabahId);
        
        $("#CustomerRedempSelectedCode").val(CIFNo);
        $("#CustomerRedempSelectedName").val(CIFName);
        $("#CustomerRedempSelectedId").val(NasabahId);
    };

    $("#CustomerRedempSelectButton").on("click", function () {
        var selectedCode = $("#CustomerRedempSelectedCode").val();
        var selectedName = $("#CustomerRedempSelectedName").val();
        var selectedId = $("#CustomerRedempSelectedId").val();

        $("input[id='srcCIFRedemp_text1']").val(selectedCode).change();
        $("input[id='srcCIFRedemp_text2']").val(selectedName);
        $("input[name='CustomerModel.NasabahId']").val(selectedId);

    });

    $("#CustomerRedempSearchComponentSearchTxt").on("keypress", function (e) {
        var key = e.which;
        if (key == 13)  // the enter key code
        {
            $("#CustomerRedempSearchComponentSearchBtn").click();
            return false;
        }
    });
</script>
<div class="modal-content">
    <div class="modal-header">
        <button type="button" class="close" data-dismiss="modal">&times;</button>
        <h4>@ViewBag.Title</h4>
    </div>
    <div class="modal-body">
        @(Html.Kendo().Grid<Reksa.Models.CustomerModel>()
                    .Name("CustomerRedempSearchComponentGrid")
                    .Columns(columns =>
                    {
                        columns.Bound(m => m.CIFNo);
                        columns.Bound(m => m.CIFName);
                        columns.Bound(m => m.NasabahId);
                    })
                    .ToolBar(toolbar =>
                    {
                        toolbar.ClientTemplateId("GridToolbarTemplate");
                    })
                    .Events(events => events.Change("onRowCustomerRedempSelect"))
                    .Pageable()
                    .Scrollable()
                    .Selectable(selectable => selectable
                        .Mode(GridSelectionMode.Single))
                    .DataSource(dataSource => dataSource
                        .Ajax()
                        .PageSize(20)
                        .Model(model =>
                        {
                            model.Id(m => m.CIFNo);
                        })
                        .Read(read => read.Action("SearchCustomerData", "Global", new { search = search, criteria = criteria }))
                    )
        )

        <script id="GridToolbarTemplate">
        <div class="toolbar">
            @(Html.Kendo().TextBox().Name("CustomerRedempSearchComponentSearchTxt").Value(search).ToClientTemplate())
            @(Html.Kendo().Button()
                            .HtmlAttributes(new { type = "button" })
                            .Name("CustomerRedempSearchComponentSearchBtn")
                            .Content("Search")
                            .Events(ev => ev.Click("onSearchCustomerRedemp")).ToClientTemplate()
            )
        </div>
        </script>

        <input type="hidden" id="CustomerRedempSelectedCode" name="CustomerRedempSelectedCode" value="" />
        <input type="hidden" id="CustomerRedempSelectedName" name="CustomerRedempSelectedName" value="" />
        <input type="hidden" id="CustomerRedempSelectedId" name="CustomerRedempSelectedId" value="" />
    </div>
    <div class="modal-footer">
        <button type="submit" id="CustomerRedempSelectButton" class="btn btn-primary pull-left" data-dismiss="modal">
            <span class="glyphicon glyphicon-ok"></span> Select
        </button>
        <button type="submit" class="btn btn-warning pull-left" data-dismiss="modal">
            <span class="glyphicon glyphicon-remove"></span> Cancel
        </button>
    </div>
</div>
