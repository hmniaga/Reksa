@{
    ViewBag.Title = "Search Product";
    string search = (string)ViewBag.Search;
    string criteria = (string)ViewBag.Criteria;

    Layout = null;
}
<script type="text/javascript">
    function onSearchTrxProductSwcOut() {
        var value = $("#TrxProductSwcOutSearchComponentSearchTxt").val();
        var grid = $("#TrxProductSwcOutSearchComponentGrid").data("kendoGrid");

        var parsedUrl = updateQueryStringParameter(grid.dataSource.transport.options.read.url, "search", value);
        grid.dataSource.transport.options.read.url = parsedUrl;

        grid.dataSource.page(1);
        grid.dataSource.read();
        grid.refresh();
    }

    function updateQueryStringParameter(uri, key, value) {
        var re = new RegExp("([?&])" + key + "=.*?(&|$)", "i");
        var separator = uri.indexOf('?') !== -1 ? "&" : "?";
        if (uri.match(re)) {
            return uri.replace(re, '$1' + key + "=" + value + '$2');
        }
        else {
            return uri + separator + key + "=" + value;
        }
    }

    function onRowTrxProductSwcOutSelect(e) {
        var data = this.dataItem(this.select());
        var ProdCode = $.trim(data.ProdCode);
        var ProdName = $.trim(data.ProdName);
        var ProdId = $.trim(data.ProdId);
        var ProdCurr = $.trim(data.ProdCCY);

        $("#TrxProductSwcOutSelectedCode").val(ProdCode);
        $("#TrxProductSwcOutSelectedName").val(ProdName);
        $("#TrxProductSwcOutSelectedId").val(ProdId);
        $("#TrxProductSwcOutSelectedCurr").val(ProdCurr);
    };

    $("#TrxProductSwcOutSelectButton").on("click", function () {
        var selectedCode = $("#TrxProductSwcOutSelectedCode").val();
        var selectedName = $("#TrxProductSwcOutSelectedName").val();
        var selectedId = $("#TrxProductSwcOutSelectedId").val();
        var selectedCurr = $("#TrxProductSwcOutSelectedCurr").val();

        var res = ValidateCurrency(selectedCurr);

        res.success(function (data) {
            if (data.length != 0) {
                $("input[id='srcCurrencySwcOut_text2']").val(data[0].CurrencyName);
            } else {
                $("input[id='srcCurrencySwcOut_text2']").val('');
            }
        });

        $("input[id='srcProductSwcOut_text1']").val(selectedCode).change();
        $("input[id='srcProductSwcOut_text2']").val(selectedName);
        $("input[id='ProdIdSwcOut']").val(selectedId);
        $("input[id='ProdCCYSwcOut']").val(selectedCurr);
        $("input[id='srcCurrencySwcOut_text1']").val(selectedCurr);
    });

    $("#TrxProductSwcOutSearchComponentSearchTxt").on("keypress", function (e) {
        var key = e.which;
        if (key == 13)  // the enter key code
        {
            $("#TrxProductSwcOutSearchComponentSearchBtn").click();
            return false;
        }
    });

    function getData() {
        var Search = "@ViewBag.Search";
        var criter = "@ViewBag.Criteria";
        return {
            search: Search,
            criteria: encodeURIComponent(criter)
        }
    }

    function ValidateCurrency(CurrCode) {
        return $.ajax({
            type: 'GET',
            url: '/Global/ValidateCurrency',
            data: { Col1: CurrCode, Col2: '', Validate: 1 }
        });
    }
</script>
<div class="modal-content">
    <div class="modal-header">
        <button type="button" class="close" data-dismiss="modal">&times;</button>
        <h4>@ViewBag.Title</h4>
    </div>
    <div class="modal-body">
        @(Html.Kendo().Grid<Reksa.Models.TransaksiProduct>()
                                            .Name("TrxProductSwcOutSearchComponentGrid")
                                            .Columns(columns =>
                                            {
                                                columns.Bound(m => m.ProdCode).Width(100).Title("Product Code");
                                                columns.Bound(m => m.ProdName).Width(300).Title("Product Name");
                                                columns.Bound(m => m.ProdId).Hidden();
                                                columns.Bound(m => m.ProdCCY).Hidden();
                                            })
                                            .ToolBar(toolbar =>
                                            {
                                                toolbar.ClientTemplateId("GridToolbarTemplate");
                                            })
                                            .Events(events => events.Change("onRowTrxProductSwcOutSelect"))
                                            .Pageable()
                                            .Scrollable()
                                            .Selectable(selectable => selectable
                                                .Mode(GridSelectionMode.Single))
                                            .DataSource(dataSource => dataSource
                                                .Ajax()
                                                .PageSize(20)
                                                .Model(model =>
                                                {
                                                    model.Id(m => m.ProdCode);
                                                })
                                                .Read(read =>
                                                read.Action("SearchTrxProductData", "Global").Data("getData"))
                                            )
        )


        <script id="GridToolbarTemplate">
        <div class="toolbar">
            @(Html.Kendo().TextBox().Name("TrxProductSwcOutSearchComponentSearchTxt").Value(search).ToClientTemplate())
            @(Html.Kendo().Button()
                            .HtmlAttributes(new { type = "button" })
                            .Name("TrxProductSwcOutSearchComponentSearchBtn")
                            .Content("Search")
                            .Events(ev => ev.Click("onSearchTrxProductSwcOut")).ToClientTemplate()
            )
        </div>
        </script>

        <input type="hidden" id="TrxProductSwcOutSelectedCode" name="TrxProductSwcOutSelectedCode" value="" />
        <input type="hidden" id="TrxProductSwcOutSelectedName" name="TrxProductSwcOutSelectedName" value="" />
        <input type="hidden" id="TrxProductSwcOutSelectedId" name="TrxProductSwcOutSelectedCId" value="" />
        <input type="hidden" id="TrxProductSwcOutSelectedCurr" name="TrxProductSwcOutSelectedCurr" value="" />
        
    </div>
    <div class="modal-footer">
        <button type="submit" id="TrxProductSwcOutSelectButton" class="btn btn-primary pull-left" data-dismiss="modal">
            <span class="glyphicon glyphicon-ok"></span> Select
        </button>
        <button type="submit" class="btn btn-warning pull-left" data-dismiss="modal">
            <span class="glyphicon glyphicon-remove"></span> Cancel
        </button>
    </div>
</div>
