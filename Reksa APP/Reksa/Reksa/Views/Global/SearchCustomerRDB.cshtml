@{
    ViewBag.Title = "Search CIF";
    string search = (string)ViewBag.Search;
    string criteria = (string)ViewBag.Criteria;

    Layout = null;
}
<script type="text/javascript">
    function onSearchCustomerRDB() {
        var value = $("#CustomerRDBSearchComponentSearchTxt").val();
        var grid = $("#CustomerRDBSearchComponentGrid").data("kendoGrid");

        var parsedUrl = updateQueryStringParameter(grid.dataSource.transport.options.read.url, "search", value);
        grid.dataSource.transport.options.read.url = parsedUrl;

        grid.dataSource.page(1);
        grid.dataSource.read();
        grid.refresh();
    }

    function updateQueryStringParameter(uri, key, value) {
        var re = new RegExp("([?&])" + key + "=.*?(&|$)", "i");
        var separator = uri.indexOf('?') !== -1 ? "&" : "?";
        if (uri.match(re)) {
            return uri.replace(re, '$1' + key + "=" + value + '$2');
        }
        else {
            return uri + separator + key + "=" + value;
        }
    }

    function onRowCustomerRDBSelect(e) {
        var data = this.dataItem(this.select());
        var CIFNo = $.trim(data.CIFNo);
        var CIFName = $.trim(data.CIFName);
        var NasabahId = $.trim(data.NasabahId);
        
        $("#CustomerRDBSelectedCode").val(CIFNo);
        $("#CustomerRDBSelectedName").val(CIFName);
        $("#CustomerRDBSelectedId").val(NasabahId);
    };

    $("#CustomerRDBSelectButton").on("click", function () {
        var selectedCode = $("#CustomerRDBSelectedCode").val();
        var selectedName = $("#CustomerRDBSelectedName").val();
        var selectedId = $("#CustomerRDBSelectedId").val();

        $("input[id='srcCIFRDB_text1']").val(selectedCode).change();
        $("input[id='srcCIFRDB_text2']").val(selectedName);
        $("input[name='CustomerModel.NasabahId']").val(selectedId);

    });

    $("#CustomerRDBSearchComponentSearchTxt").on("keypress", function (e) {
        var key = e.which;
        if (key == 13)  // the enter key code
        {
            $("#CustomerRDBSearchComponentSearchBtn").click();
            return false;
        }
    });
</script>
<div class="modal-content">
    <div class="modal-header">
        <button type="button" class="close" data-dismiss="modal">&times;</button>
        <h4>@ViewBag.Title</h4>
    </div>
    <div class="modal-body">
        @(Html.Kendo().Grid<Reksa.Models.CustomerModel>()
                    .Name("CustomerRDBSearchComponentGrid")
                    .Columns(columns =>
                    {
                        columns.Bound(m => m.CIFNo);
                        columns.Bound(m => m.CIFName);
                        columns.Bound(m => m.NasabahId);
                    })
                    .ToolBar(toolbar =>
                    {
                        toolbar.ClientTemplateId("GridToolbarTemplate");
                    })
                    .Events(events => events.Change("onRowCustomerRDBSelect"))
                    .Pageable()
                    .Scrollable()
                    .Selectable(selectable => selectable
                        .Mode(GridSelectionMode.Single))
                    .DataSource(dataSource => dataSource
                        .Ajax()
                        .PageSize(20)
                        .Model(model =>
                        {
                            model.Id(m => m.CIFNo);
                        })
                        .Read(read => read.Action("SearchCustomerData", "Global", new { search = search, criteria = criteria }))
                    )
        )

        <script id="GridToolbarTemplate">
        <div class="toolbar">
            @(Html.Kendo().TextBox().Name("CustomerRDBSearchComponentSearchTxt").Value(search).ToClientTemplate())
            @(Html.Kendo().Button()
                            .HtmlAttributes(new { type = "button" })
                            .Name("CustomerRDBSearchComponentSearchBtn")
                            .Content("Search")
                            .Events(ev => ev.Click("onSearchCustomerRDB")).ToClientTemplate()
            )
        </div>
        </script>

        <input type="hidden" id="CustomerRDBSelectedCode" name="CustomerRDBSelectedCode" value="" />
        <input type="hidden" id="CustomerRDBSelectedName" name="CustomerRDBSelectedName" value="" />
        <input type="hidden" id="CustomerRDBSelectedId" name="CustomerRDBSelectedId" value="" />
    </div>
    <div class="modal-footer">
        <button type="submit" id="CustomerRDBSelectButton" class="btn btn-primary pull-left" data-dismiss="modal">
            <span class="glyphicon glyphicon-ok"></span> Select
        </button>
        <button type="submit" class="btn btn-warning pull-left" data-dismiss="modal">
            <span class="glyphicon glyphicon-remove"></span> Cancel
        </button>
    </div>
</div>
